${jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Effect = "Allow"
        Principal = {
          AWS = ["arn:aws:iam::${root_account_id}:role/${config_iam_service_role_name}"]
        }
        Action   = ["sns:Publish"]
        Resource = "${sns_topic_arn}"
        Condition = {
          StringEquals = {
            "aws:PrincipalOrgID" = ["${org_id}"]
          }
        }
      }
    ]
  })}
